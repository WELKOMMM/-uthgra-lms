[project]
name = "uthgra_lms"
authors = [{ name = "UTHGRA", email = "admin@welkomia.com" }]
description = "UTHGRA customizations for Frappe LMS (WhatsApp required at signup)"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.frappe-dependencies]
frappe = ">=15.0.0"
lms = ">=2.0.0"
